name: randomization-tests
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.10
  - statsmodels>=0.14
  - scikit-learn>=1.3
  - pytest>=7.0
  - mypy>=1.0
  - ruff>=0.4
  - pre-commit
  - matplotlib
  - pip
  - pip:
      - jax>=0.4
      - jaxlib>=0.4
      - polars>=0.20
      - pyarrow
      - ucimlrepo
